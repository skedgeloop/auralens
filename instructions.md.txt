3 browser-based, 100% free and unlimited tools (no APIs, keys, or servers required) along with system prompts you can feed into an AI coding agent (like Cursor, Claude, ChatGPT, or Bolt.new) to generate the app.1. Quick Comparison SummaryToolPrimary Use CaseOutput TypeDownload SizeBest ForMediaPipe Face LandmarkerMicro-expressions & precise face tracking478 3D points + 52 Blendshapes~2.5 MBCalculating custom "Sassiness" via eyebrows/smirks@vladmandic/face-apiDirect emotion recognitionRaw Probabilities (Happy, Sad, Angry, etc.)< 1 MBOut-of-the-box happiness/sadness scoresHugging Face transformers.jsAI Vision models in JSZero-shot Image Labels / Vibe Tags~20–40 MBCustom aura classification & text tags2. Prompts to Build Each ToolCopy and paste these prompts directly into your AI code builder.Prompt 1: MediaPipe (Best for "Sassiness" & Expression Tracking)PlaintextBuild a single-file HTML/JS app using MediaPipe Face Landmarker via CDN ES Modules. 

Requirements:
1. Allow the user to upload an image or turn on their webcam.
2. Load `@mediapipe/tasks-vision` in the browser with `outputFaceBlendshapes: true`.
3. Read facial blendshapes (like eyebrow raises and mouth curls) and convert them into three percentage scores:
   - Happiness (based on mouthSmile)
   - Sadness (based on mouthFrown)
   - Sassiness (custom math: raised eyebrow + asymmetric smirk)
4. Display a colorful "Aura Card" with an aura color gradient generated from the scores.
5. Must be 100% frontend client-side code with zero external backend APIs or keys.
Prompt 2: @vladmandic/face-api (Easiest Pure Emotion Scores)PlaintextBuild a light client-side web app using `@vladmandic/face-api` loaded via CDN.

Requirements:
1. Import `face-api.esm.js` and load `tinyFaceDetector` and `faceExpressionNet` models from CDN.
2. Let the user upload a photo.
3. Automatically detect the face and extract expression probabilities: Happy, Sad, Surprised, Neutral, Angry.
4. Calculate:
   - Happiness %
   - Sadness %
   - Sassiness % (derived by combining Surprised + Neutral + low Happy)
5. Render a quirky "Mood & Aura Report" displaying these percentages along with a dynamic background color that changes based on the dominant emotion.
6. Make it run entirely in the user's browser with zero backend or API keys.
Prompt 3: Transformers.js (Best for Vibe Classification)PlaintextBuild a pure frontend HTML/JS web application using `@huggingface/transformers` loaded via CDN modules.

Requirements:
1. Import `pipeline` from `@huggingface/transformers`.
2. Initialize a `zero-shot-image-classification` vision model (e.g., CLIP) directly in the browser.
3. Allow the user to upload a photo and test it against custom text candidate labels: "happy energy", "sad mood", "sassy aura", "chaotic vibe".
4. Display the top matching aura label as a percentage match, complete with a quirky description.
5. Ensure all model inference happens locally inside the browser using WebGL/WebGPU with zero API keys or backend calls.